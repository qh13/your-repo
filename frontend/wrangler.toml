name = "jable-frontend"
compatibility_date = "2025-01-01"

[build]
command = "npm run build"
cwd = "frontend"

# 使用纯 Worker 模式（不部署静态文件）
pages_build_output_dir = ".worker"

# 主 Worker 入口
main = "_worker/index.js"

# 路由配置 - 所有请求通过 Worker 处理
[[routes]]
pattern = "/*"
script = "_worker/index"
